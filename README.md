# BlackBox — Autonomous Data Incident Response

Hackathon project. See `docs/` for requirements and `pipeline/README.md` for the
deterministic demo data fixture.

Quick start:

```bash
make setup        # uv sync
make demo-reset   # seed the incident fixture + build the warehouse
make test         # run pipeline invariants
```
